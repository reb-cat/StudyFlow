# StudyFlow Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to version control

# REQUIRED - Node Environment
NODE_ENV=development

# REQUIRED - Server Configuration
PORT=5000

# REQUIRED - Session Security (CRITICAL - Generate a strong secret)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your_secure_session_secret_here_at_least_32_characters_long

# REQUIRED - Authentication
# Use a strong password for family access
FAMILY_PASSWORD=your_secure_family_password_here

# REQUIRED - Database
# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host:port/database

# OPTIONAL - Canvas Integration
CANVAS_BASE_URL=https://your.canvas.instance.com
CANVAS_BASE_URL_2=https://your.second.canvas.instance.com
ABIGAIL_CANVAS_TOKEN=your_canvas_token_for_abigail
ABIGAIL_CANVAS_TOKEN_2=your_second_canvas_token_for_abigail
KHALIL_CANVAS_TOKEN=your_canvas_token_for_khalil

# OPTIONAL - Email Configuration (Resend)
RESEND_API_KEY=your_resend_api_key
RESEND_FROM=your_verified_sender@yourdomain.com
PARENT_EMAIL=parent@example.com

# OPTIONAL - Text-to-Speech (ElevenLabs)
ELEVEN_LABS_API_KEY=your_elevenlabs_api_key

# OPTIONAL - Object Storage (Replit)
PRIVATE_OBJECT_DIR=/path/to/private/objects
PUBLIC_OBJECT_SEARCH_PATHS=/path/to/public/objects

# OPTIONAL - Seeding Control
# Set to 'off' in production, 'once' for one-time seeding, or 'always' for development
RUN_SEEDS=off

# OPTIONAL - Production Settings
REPLIT_DEPLOYMENT=0
REPLIT_DOMAINS=your-app.replit.dev

# Security Notes:
# 1. SESSION_SECRET should be at least 32 characters and cryptographically random
# 2. FAMILY_PASSWORD should be at least 12 characters in production
# 3. All API keys should be kept secure and rotated regularly
# 4. Never share these values or commit them to version control
# 5. Use environment-specific files (.env.local, .env.production) as needed